# PRD: 냉장고 재료 기반 장보기·레시피 추천 서비스 (가칭 "냉장고를 부탁해")

- 문서 상태: Draft v0.1 (Q&A 기반 초안 — 추가 논의로 계속 보완 예정)
- 작성일: 2026-08-24
- 작성자: 서인
- 관련 프로젝트: 서비스 기획

---

## 1. 개요

사용자가 재료를 검색해 **장바구니**(구매 예정) 또는 **냉장고**(보유 재료)에 담고, 냉장고에 있는 재료를 기반으로 만들 수 있는 레시피를 자동 추천받는 웹 서비스다. 레시피를 선택해 요리를 완료하면 사용한 재료의 용량을 수정하거나 소진 처리할 수 있고, 냉장고의 각 재료는 소비기한을 자동으로 카운트해 임박 시 경고를 표시한다.

핵심 가치는 "냉장고에 뭐가 있더라?"와 "이걸로 뭘 해먹지?"를 하나의 흐름으로 해결하고, 재료를 사서 쟁여두기만 하고 상하게 버리는 낭비를 소비기한 알림으로 줄이는 것이다.

## 2. 목표

- 재료 검색 → 장바구니/냉장고 담기 → 레시피 추천까지 하나의 서비스 안에서 끊김 없이 이어진다.
- 냉장고 재료를 기준으로 실제로 만들 수 있는(또는 거의 만들 수 있는) 레시피를 우선적으로 보여준다.
- 소비기한 임박 재료를 놓치지 않도록 목록에서 바로 확인할 수 있게 한다.
- 반응형 웹으로 모바일/PC 모두 무리 없이 동작한다.

성공 지표(제안): 재료 검색~장바구니/냉장고 담기 완료까지 평균 소요 시간, 냉장고 진입 후 추천 레시피 클릭률, 요리완료(재료 소진) 처리율, 소비기한 경고 노출 후 재료 소진/삭제 전환율.

## 3. 타겟 사용자

집에 있는 재료로 뭘 해먹을지 매번 고민되는 1인·소규모 가구 사용자, 장 볼 때 필요한 재료를 놓치지 않고 싶은 사용자, 재료를 사놓고 상해서 버리는 경험이 잦은 사용자.

## 4. 범위 (Scope)

**MVP 포함**: 재료 검색(자동완성), 장바구니 담기(비회원 가능), 냉장고 담기(회원 전용), 장바구니 재료 목록(가나다순 정렬 + 검색), 냉장고 재료 목록(장바구니에 담은 횟수 기준 정렬 + 검색), 장바구니 "구매완료" → 냉장고 자동 이동, 냉장고 기반 레시피 추천(일치율 기준, 미리보기 10개 + 더보기), 레시피 상세(주요 재료 상단 고정), 레시피 즐겨찾기(☆), "요리완료" 버튼 → 사용 재료 용량수정/소진 팝업, 소진 시 냉장고에서 삭제, 소비기한 카운트 및 임박 경고, 카카오/구글 소셜 로그인, 비회원 기능 제한 시 로그인 모달, 반응형 레이아웃(PC/모바일).

**MVP 이후**: 재료 다중 검색어 저장(자주 찾는 재료), 레시피 재료 매칭 동의어 사전 지속 보완, 즐겨찾기 레시피 전용 목록 화면 세부 UX, 소비기한 알림 푸시/이메일, 재료 공유(가족 간 냉장고 공유), 레시피 리뷰/평점, 다국어 지원, 다크모드.

## 5. 핵심 기능 요구사항

**5.1 재료 검색**: 재료마스터 데이터를 기준으로 자동완성 검색을 제공한다. 검색은 장바구니/냉장고에 담기 이전 단계(검색 결과에서 바로 "장바구니 담기" / "냉장고 담기" 선택)에서 항상 노출되며, 이미 담긴 장바구니·냉장고 재료 목록 화면에서도 동일한 검색창으로 목록 내 검색이 가능하다.

**5.2 장바구니**: 비회원도 이용 가능. 재료를 여러 개(수량) 담을 수 있다. 재료 목록은 **가나다순**으로 정렬한다. "구매완료" 버튼을 누르면 담긴 재료들이 담은 수량만큼 초기 용량에 반영되어 냉장고로 자동 이동하고, 장바구니에서는 삭제된다(회원만 가능 — 비회원이 구매완료를 시도하면 로그인 모달 노출). 비회원의 장바구니 데이터는 `localStorage`에만 저장하며, 브라우저/기기 변경 시 유지되지 않는다.

**5.3 나만의 냉장고 (회원 전용)**: 재료 목록은 **사용자가 장바구니에 담은 횟수가 많은 순**으로 정렬한다. 재료별 초기 용량은 재료마스터의 최소단위를 기준으로 하되(예: 대파 1단, 돼지고기 1근), 과일류처럼 표준 단위가 모호한 품목은 재료마스터에서 개별적으로 500g~1kg 범위 내 기준값을 정의한다. 장바구니에서 여러 개 담아 이동한 경우 담은 수량만큼 초기 용량이 누적 반영된다(예: 대파 2개 구매완료 → 냉장고에 대파 2단).

**5.4 소비기한 카운트**: 냉장고에 추가된 각 재료는 재료마스터의 적정 소비기한을 기준으로 남은 일수를 목록 오른쪽 끝에 표시한다. 마감까지 **2일 이하**로 남으면 "재료가 상할수도 있어요!"와 같은 경고 문구를 소비기한 표시 영역에 노출한다.

**5.5 레시피 추천**: 냉장고 재료를 기준으로, 레시피의 주요 재료 중 **일정 비율(예: 70%) 이상**이 냉장고에 있는 레시피를 우선 추천한다. 부족한 재료가 1~2개인 경우 "재료 추가 필요" 형태로 부족분을 함께 표시한다. 추천 목록은 냉장고 화면 하단에 미리보기 카드 10개를 노출하고, 각 카드는 **이미지 + 레시피명 + 일치율(%) + 부족 재료 개수**만 간단히 표시한다. "레시피 더보기" 버튼을 누르면 10개씩 추가로 로딩된다. 조건에 맞는 레시피가 없으면 "추천 레시피가 없습니다" 문구를 표시한다.

**5.6 레시피 상세 및 즐겨찾기**: 레시피를 열람하면 그 레시피의 주요 재료가 화면(재료 목록) 상단에 고정 노출된다(즐겨찾기 여부와 무관하게 열람할 때마다 동작). 즐겨찾기(☆)는 별도의 "자주 보는 레시피 저장" 기능으로, 상단 고정 기능과는 독립적으로 동작한다.

**5.7 요리완료 및 재료 소진 처리**: 레시피 상세 화면에 "요리완료" 버튼을 두고, 누르면 "재료를 수정하시겠습니까?" 안내 문구와 함께 해당 레시피에 사용된 재료만 팝업으로 노출한다. 각 재료는 **용량 수정** 또는 **소진** 중 선택할 수 있으며, 소진을 선택하면 해당 재료는 나만의 냉장고 목록에서 삭제된다.

**5.8 회원 인증 및 접근 제어**: 카카오·구글 소셜 로그인을 지원한다. 장바구니 담기는 비회원도 이용 가능하며, 그 외 모든 기능(냉장고 담기, 레시피 추천/상세/즐겨찾기, 요리완료 처리)은 회원 전용이다. 비회원이 회원 전용 기능을 클릭하면 그 시점에 로그인 모달을 띄워 자연스럽게 로그인을 유도한다.

## 6. 정보 구조 / 화면 흐름

홈/검색(재료 자동완성 검색) → 검색 결과에서 "장바구니 담기" 또는 "냉장고 담기" 선택 → **[장바구니]** 재료 목록(가나다순, 검색 가능) → 구매완료 → 냉장고로 이동 / **[냉장고]** 재료 목록(장바구니 담은 횟수순, 검색 가능, 소비기한 카운트+경고) → 하단 추천 레시피 미리보기 10개(더보기 시 10개씩 추가) → 레시피 클릭 → 상세(주요 재료 상단 고정, ☆ 즐겨찾기) → 요리완료 버튼 → 사용 재료 수정/소진 팝업 → 소진 선택 시 냉장고에서 삭제. 비회원이 냉장고/즐겨찾기/요리완료 등을 시도하면 로그인 모달로 분기.

## 7. 데이터 소스 / API 연동

**7.1 레시피 데이터**: 식품의약품안전처 "조리식품의 레시피 DB"(공공데이터포털) 사용. 무료·안정적이지만 레시피 수가 적고 재료 정보가 단순 텍스트로 제공되어 그대로는 매칭 정확도가 낮다.

**7.2 재료마스터 데이터(표준 재료명·최소단위·적정 소비기한)**: 식품의약품안전처 "소비기한 참고값" 공공데이터를 기본으로 활용하고, 여기 없는 재료 항목은 직접 보완해 구축한다.

**7.3 레시피-재료마스터 매칭**: 개발 착수 시 LLM API(Gemini/Claude 등)로 레시피 DB의 재료 텍스트를 일괄 파싱해 재료마스터 ID로 매핑하는 테이블을 1회성으로 생성한다. 이후 서비스 운영 중에는 이 매핑 결과를 DB에 저장해두고 재사용하며, 매번 실시간으로 LLM을 호출하지 않는다. 신규 레시피가 추가될 경우에만 추가 파싱이 필요하다.

**7.4 인증**: Supabase Auth(또는 동등한 서비스)를 통해 카카오·구글 OAuth 로그인을 구현한다.

## 8. 기술 스택 (확정: React(Vite) + Spring Boot + AWS EC2)

- **프론트**: Vite+React(SPA). Next.js의 SSR·서버 라우팅은 이 서비스 특성상(로그인 후 개인화 대시보드형, SEO 비중 낮음) 불필요해 더 가벼운 Vite+React로 구성.
- **백엔드**: Spring Boot(Java). Spring Security + OAuth2 Client로 카카오/구글 로그인, JPA/Hibernate로 회원-장바구니-냉장고-레시피즐겨찾기 등 연관관계 매핑. 소비기한 일일 재계산은 `@Scheduled` 배치로 처리.
- **DB**: PostgreSQL(Supabase 또는 EC2 내 자체 호스팅 중 택1 — 12절 오픈 이슈 참고).
- **배포**: AWS EC2(Spring Boot는 상시 구동 JVM 프로세스가 필요해 Vercel 서버리스에는 배포 불가). DuckDNS + Let's Encrypt, GitHub Actions + GHCR CI/CD 등 Linkpan과 유사한 인프라 패턴 재사용 가능.
- **AWS 프리티어 관련 참고**: 2025년 7월 15일 이후 신규 계정은 기존 "12개월간 매달 750시간 무료" 구조가 아니라 **가입 시 크레딧($100~200)을 받고 6개월 또는 크레딧 소진 시 계정이 자동 종료**되는 구조로 변경되었다. 여러 계정을 운영해 프리티어를 이어가는 방식을 계획 중이라면, 계정별 6개월 주기와 크레딧 소진 시점을 미리 트래킹해 서비스 중단 없이 이전(migration)하는 절차를 마련해야 한다. AWS 이용약관상 크레딧은 동일 고객에게 계정 1개 초과로 제공되지 않는다는 문구가 있어 정책 준수 여부도 함께 고려한다.
- **이력서 관점**: 기존 프로젝트가 대부분 Python(FastAPI) 기반이라, 이 프로젝트를 Spring Boot로 구현하면 Java 백엔드 실무 패턴(계층형 아키텍처, JPA, Spring Security) 경험을 포트폴리오에 추가할 수 있다.
- **포트폴리오 연동**: Portfolio Hub의 프로젝트 카드에서는 배포 위치(EC2/DuckDNS 도메인)와 무관하게 URL 링크만 연결하면 되므로 별도 제약 없음.

## 9. 비기능 요구사항

반응형 웹(PC/모바일 대응), 재료·레시피 검색은 즉각적인 자동완성 반응 속도 확보, API 실패가 서비스 전체 중단으로 이어지지 않도록 화면 단위로 에러 격리, 소비기한 계산은 매일 자동 갱신, 비회원 localStorage 데이터와 회원 서버 데이터 간 정합성 문제(로그인 전환 시 장바구니 이관 여부)는 8절 오픈 이슈 참고.

## 10. 오픈 이슈

- 과일류 등 표준 단위가 모호한 재료의 최소단위(500g~1kg 범위)를 재료별로 정확히 어떻게 큐레이션할지 세부 기준.
- 비회원 상태에서 담은 장바구니(localStorage)를 로그인 직후 회원 계정으로 이관할지 여부(현재는 이관 없음으로 가정, 필요 시 재논의).
- 즐겨찾기(☆) 레시피만 모아보는 전용 목록 화면의 유무 및 위치.
- 레시피 추천 일치율 기준치(제안: 70%)의 최종 확정 및 부족 재료 표시 UI 세부안.
- 소비기한 경고 임계값(2일)을 재료 카테고리별로 다르게 둘지, 전체 공통으로 둘지.
- 재료마스터-레시피 매핑에 사용할 LLM API의 정확한 모델·비용 산정(개발 착수 시 재검증).
- DB를 Supabase(관리형)로 둘지, EC2 내 자체 PostgreSQL로 둘지 — 후자는 EC2 계정 이전 시 DB 데이터도 함께 마이그레이션해야 하는 부담이 있음.
- AWS 프리티어 신규 정책(6개월 크레딧 소진 시 계정 자동 종료)에 맞춘 계정 순환·마이그레이션 절차 설계(도메인 유지, 데이터 백업/복원 방식 포함).
- 조리식품의 레시피 DB의 정확한 엔드포인트·파라미터 스펙 재검증(공공데이터포털 문서 기준).

## 11. 마일스톤(제안)

M1: 재료마스터 데이터 구축(소비기한 공공데이터 기반) + 레시피 DB 연동 및 LLM 매핑 테이블 생성
M2: 정적 UI(검색, 장바구니, 냉장고 목록) + 소셜 로그인(Spring Security + OAuth2) 연동
M3: 장바구니↔냉장고 이동 로직, 소비기한 카운트/경고 UI(스케줄러 배치)
M4: 레시피 추천 로직(일치율 계산) + 추천 리스트/상세 화면
M5: 요리완료(재료 수정/소진 팝업), 즐겨찾기 기능
M6: 반응형 대응, QA, EC2 배포(도메인·CI/CD 구성) 및 프리티어 계정 마이그레이션 절차 정리
