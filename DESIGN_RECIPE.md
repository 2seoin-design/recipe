# DESIGN: 냉장고 재료 기반 장보기·레시피 추천 서비스

- 문서 상태: Draft v0.1
- 작성일: 2026-08-24
- 작성자: 서인
- 관련 문서: PRD_냉장고레시피_서비스.md

---

## 1. 디자인 방향

배달의민족 B마트류 커머스 서비스를 참고 축으로 삼되, 컬러는 민트+베이비블루 계열로 재구성한다. B마트에서 가져오는 것은 색상이 아니라 **UI 문법**이다.

- 카드 그리드 중심의 상품(재료) 진열 — 큰 썸네일, 짧은 라벨, 정보 밀도는 낮게
- 칩(chip) 형태의 카테고리·필터 UI — 탭보다 가볍고 다중 선택에 유리
- 크고 둥근 모서리, 두꺼운 굵기의 우호적인 타이포그래피 — 장보기가 숙제가 아니라 가벼운 일처럼 느껴지게
- 배지·라벨로 상태를 즉시 전달 — 재고 없음, 소비기한 임박, 일치율 등 숫자·상태 정보를 시각적으로 눈에 띄게 배치

이 서비스만의 차별점은 "커머스"보다 "내 냉장고"라는 감각이다. B마트가 새 물건을 파는 흥분감이라면, 이 서비스는 이미 가진 것을 잘 쓰게 돕는 안도감에 가깝다. 그래서 채도는 B마트보다 한 톤 낮추고, 경고(소비기한)만큼은 명확하게 튀도록 대비를 확보한다.

## 2. 컬러 팔레트

공식 색조는 **민트(주색)**와 **베이비블루(보조색)** 두 축으로 구성하고, 여기에 중립색·경고색을 더한다.

| 역할 | 이름 | HEX | 용도 |
|---|---|---|---|
| Primary | Mint 500 | `#2FBF9B` | 메인 버튼, 활성 탭, 브랜드 포인트 |
| Primary Dark | Mint 700 | `#1E8C72` | 버튼 hover/active, 강조 텍스트 |
| Primary Tint | Mint 50 | `#E8F9F4` | 카드 배경, 선택된 칩 배경 |
| Secondary | Baby Blue 400 | `#8FC7F2` | 보조 배지, 정보성 강조, 그래픽 요소 |
| Secondary Tint | Baby Blue 50 | `#EFF7FE` | 섹션 배경, 냉장고 화면 배경 |
| Neutral Text | Ink 900 | `#1C2426` | 본문 텍스트 |
| Neutral Sub | Ink 500 | `#5C6B6E` | 보조 텍스트, 캡션 |
| Neutral Surface | Fog 50 | `#F6FAF9` | 전체 배경(민트를 아주 옅게 섞은 오프화이트) |
| Warning | Amber 500 | `#F2A93C` | 부족 재료·일반 주의 |
| Danger | Coral 600 | `#E4573D` | 소비기한 임박 경고(2일 이하) |

**대비 검증(요약)**: Ink 900(#1C2426) 위 흰 배경 대비 15.1:1, Mint 700(#1E8C72) × 흰 배경 4.6:1(버튼 텍스트에 흰색 사용 가능), Coral 600 × 흰 배경 4.1:1로 텍스트 단독 사용 시 기준(4.5:1)에 약간 못 미쳐 **경고 문구는 굵게(bold) 처리하거나 배경 위 큰 텍스트(18px+)로만 사용**한다. 나머지 조합은 4.5:1 이상을 만족한다.

**컬러 사용 규칙**: 민트는 "지금 할 수 있는 행동"(버튼, 활성 상태)에, 베이비블루는 "정보 전달"(배지, 배경 섹션 구분)에 사용해 두 색의 역할을 혼동하지 않는다. 경고색(Coral)은 소비기한 임박에서만 배타적으로 사용해 다른 곳에 쓰지 않는다 — 그래야 사용자가 이 색을 보는 순간 "확인해야 한다"고 즉시 인지한다.

## 3. 타이포그래피

- **본문/UI 전체**: Pretendard (self-hosted). 굵기는 Regular(400)/Medium(500)/SemiBold(600)/Bold(700)을 상황별로 사용.
- **숫자 강조(일치율%, 소비기한 D-day, 가격)**: Pretendard Bold + 자간을 살짝 좁혀(-1%) 숫자가 카드 안에서 눈에 띄게. 별도 숫자 전용 폰트는 두지 않아 로딩 비용을 늘리지 않는다.
- **타입 스케일(모바일 기준)**:
  - Display(페이지 타이틀): 22px / Bold / line-height 1.3
  - Section 제목: 18px / SemiBold
  - 카드 타이틀(재료명·레시피명): 15px / SemiBold
  - 본문/설명: 14px / Regular
  - 캡션(소비기한, 부가정보): 12px / Medium

버튼과 배지 텍스트는 항상 SemiBold 이상을 사용해, 얇은 글씨가 주는 사무적인 인상 대신 친근하고 눌러보고 싶은 느낌을 준다.

## 4. 레이아웃 원칙

**전역**: 최대 컨텐츠 너비 480px 기준 모바일 우선 설계 후 PC에서는 중앙 정렬 + 좌우 여백 확장(카드 그리드 열 수만 2→4로 증가). 라운드 처리는 카드 16px, 버튼/칩 999px(완전 pill) — 각지지 않은 형태를 전면에 사용해 B마트류의 우호적인 인상을 재현한다.

**홈/검색**: 상단 고정 검색바(베이비블루 50 배경 위 흰 입력창) + 자동완성 리스트. 검색 결과 각 항목에는 "장바구니 담기" / "냉장고 담기" 두 개의 pill 버튼을 나란히 배치(왼쪽 민트 outline = 장바구니, 오른쪽 민트 fill = 냉장고 — 회원 전용 액션을 더 강조된 스타일로 구분).

```
[ 🔍 재료 검색           ]
┌─────────────────────────┐
│ [이미지] 대파            │
│  1단 · 1,500원           │
│  [ 장바구니 ]  [ 냉장고 ] │
└─────────────────────────┘
```

**장바구니 화면**: 상단에 검색창(목록 내 검색), 그 아래 가나다순 리스트. 리스트 우측에 수량 스테퍼(- 1 +), 하단 고정 바에 "구매완료" 버튼(민트 fill, 풀 너비).

**나만의 냉장고 화면**: 배경을 베이비블루 50으로 옅게 깔아 장바구니와 화면 톤을 구분한다. 재료 목록(자주 담은 순 정렬)은 각 행 오른쪽 끝에 소비기한 D-day 배지를 배치하고, D-2 이하일 때만 Coral 배경의 강조 배지로 전환되며 "재료가 상할수도 있어요!" 텍스트가 배지 아래 작게 붙는다.

```
┌───────────────────────────────┐
│ 대파                    D-5   │
├───────────────────────────────┤
│ 돼지고기                D-1 ⚠│
│                재료가 상할수도있어요!│
└───────────────────────────────┘
```

목록 하단에 구분선과 함께 "냉장고로 만들 수 있는 레시피" 섹션 제목, 가로 스크롤 또는 2열 그리드의 레시피 카드 10개, 더보기 버튼(outline pill)이 이어진다. 매칭 결과가 없으면 카드 영역 대신 중앙 정렬된 일러스트+"추천 레시피가 없습니다" 텍스트로 대체한다.

**레시피 카드**: 이미지(4:3) 위에 좌상단 일치율 배지(민트 fill, 예: "일치율 82%"), 우상단 즐겨찾기 ☆ 아이콘(누르면 채워진 별 + 민트색으로 전환), 하단 레시피명 + 부족 재료 개수 캡션("부족한 재료 1개").

**레시피 상세**: 상단 히어로 이미지 아래 "주요 재료" 섹션이 화면에 고정(sticky) 노출되어 스크롤해도 항상 보이며, 냉장고 보유 여부에 따라 재료 칩 색이 다르다(보유: 민트 fill, 부족: 회색 outline). 하단 고정 CTA 바에 "요리완료" 버튼(민트 fill, 풀 너비).

**요리완료 팝업**: 화면 하단에서 올라오는 바텀시트. 상단에 "재료를 수정하시겠습니까?" 안내문, 그 아래 사용된 재료만 리스트로 나열하고 각 행에 "용량 수정" / "소진" 두 개의 세그먼트 버튼을 배치한다.

## 5. 컴포넌트 스타일 가이드

| 컴포넌트 | 스타일 |
|---|---|
| Primary 버튼 | Mint 500 배경, 흰 텍스트, radius 999px, 눌렀을 때 Mint 700로 전환 |
| Secondary 버튼 | 흰 배경 + Mint 500 1.5px outline, Mint 700 텍스트 |
| 칩(카테고리/필터) | 비활성: Fog 50 배경 + Ink 500 텍스트 / 활성: Mint 50 배경 + Mint 700 텍스트 + Mint 500 1px 보더 |
| 배지(일치율) | Mint 500 배경, 흰 텍스트, Bold, 소형 pill |
| 배지(소비기한 일반) | Baby Blue 50 배경, Ink 900 텍스트 |
| 배지(소비기한 임박) | Coral 600 배경, 흰 텍스트, Bold 필수(4절 대비 규칙) |
| 카드 | radius 16px, 그림자 대신 Fog 50과의 배경 대비로 구분(과한 shadow 지양 — 밝은 팔레트에서 그림자는 탁해 보이기 쉬움) |
| 입력창 | radius 12px, 흰 배경, Baby Blue 400 1px 보더, focus 시 Mint 500 2px 보더 |

## 6. 시그니처 요소

레시피 상세의 **"주요 재료 상단 고정 바"**를 이 서비스의 시그니처로 삼는다. 재료 칩이 보유/부족 여부에 따라 색이 갈리고, 스크롤 내내 화면에 붙어 있어 "지금 이 재료들만 있으면 만들 수 있다"는 확신을 계속 상기시킨다. 커머스형 서비스에서 보기 힘든, "내 것"과 "부족한 것"을 계속 대비시켜 보여주는 이 서비스만의 장치다.

## 7. 반응형 기준

- 모바일(~479px): 카드 그리드 2열, 하단 고정 CTA 바 전 화면 노출
- 태블릿(480~1023px): 카드 그리드 3열
- 데스크톱(1024px~): 카드 그리드 4열, 콘텐츠 최대 너비 1080px 중앙 정렬, 하단 고정 CTA는 카드 내부 인라인 버튼으로 전환(데스크톱에서는 고정 바 UX가 어색하므로)

## 8. 접근성

- 모든 상태 배지는 색상 단독이 아니라 텍스트(또는 아이콘)를 함께 표기한다(소비기한 임박 배지에 ⚠ 아이콘 + 텍스트 병기).
- 포커스 인디케이터는 Mint 700 2px 아웃라인으로 전체 컴포넌트에 일관 적용.
- `prefers-reduced-motion` 설정 시 바텀시트·카드 전환 애니메이션을 즉시 전환으로 대체.
- Coral 배경 위 흰 텍스트는 Bold 이상, 14px 이상 크기로만 사용(4절 대비 검증 기준 준수).

## 9. 오픈 이슈

- 레시피 카드 이미지가 공공데이터 레시피 DB에서 저해상도로 제공될 경우의 대체 처리(플레이스홀더 일러스트 방향 별도 필요).
- 다크모드는 MVP 이후로 분류되어 있어(PRD 4절), 본 문서의 색상 토큰은 라이트모드만 정의함 — 추후 다크모드 착수 시 별도 절 추가 필요.
- 시그니처 요소(주요 재료 상단 고정 바)의 모바일 sticky 구현 시 하단 CTA 바와 상단 고정 바가 화면을 과하게 잠식하지 않는지 실제 기기 테스트 필요.
