services:
  - type: web
    name: recipe-server
    runtime: docker
    dockerfilePath: server/Dockerfile
    dockerContext: server
    plan: free
    envVars:
      - key: FOODSAFETY_API_KEY
        sync: false
